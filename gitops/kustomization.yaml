apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Note: flux-kustomizations/ removed - replaced by ArgoCD Applications in argocd/applications/.
# ArgoCD individually reconciles each sub-path below; this file is kept for
# local validation only (e.g. kustomize build gitops/).
resources:
  - workspaces
